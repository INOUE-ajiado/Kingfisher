{
  "name": "Kingfisher",
  "summary": "アニメ制作の彩色・仕上げ工程をブラウザで完結させる Web アプリ。RETAS STUDIO PaintMan 互換を目指す。完全フロントエンド動作でサーバーへ画像を送らない。",
  "production": "https://kingfisher-paint-2026.web.app",
  "repository": "git@github.com:INOUE-ajiado/Kingfisher.git",
  "repositoryVisibility": "public",
  "lastReviewed": "2026-08-27",
  "techStack": {
    "implemented": [
      "React 18 + TypeScript 5.7 + Vite 6",
      "Zustand 5 (スライス分割)",
      "Tailwind 3 / lucide-react",
      "Canvas 2D API による描画 (putImageData → drawImage)",
      "File System Access API (ゼロ・アップロード)",
      "Web Workers (TGA デコード / ベクター・ラスタートレース)",
      "vitest (テスト 326 件)",
      "Firebase Hosting (COOP/COEP ヘッダー付き)",
      "撮影上がりロール (.mov / .mp4) の再生 — ネイティブ <video>。デコードは自前でやらない",
      "作業領域のレイアウト — 面 (Win A / Win B / 見本 / ロール) を横一列の枠へ割り当て、並べ替え・タブ重ね・一面表示・幅調整ができる (engine/paneLayout.ts)",
      "操作ログ (DEBUG ウィンドウ) — 右サイドバーの最下段。フォルダを開く / コマ送り / 窓の開閉 / 連動を時刻付きで記録し、全文コピーできる (engine/debugLog.ts, components/panels/DebugLogPanel.tsx)"
    ],
    "notImplemented": [
      "Rust / WebAssembly — 存在しない。画像処理は全て素の TypeScript",
      "WebGPU — src/engine/webgpuRenderer.ts は書かれているがどこからも import されていない。実際の描画は Canvas 2D",
      "SharedArrayBuffer — 未使用。COOP/COEP ヘッダーだけ先に設定済み",
      "レイヤー — LayerPanel に一覧を出すだけで描画には一切反映されない。separateLineartLayersGlobal は空のレイヤーを 3 枚足して「分離生成しました」と表示するだけ"
    ]
  },
  "criticalWarnings": [
    {
      "id": "docs-are-not-spec",
      "severity": "high",
      "title": "Doc/ の仕様書は達成済み仕様ではない",
      "detail": "Doc/ 配下の 24 本の Markdown はアジャイル開発の過程で書かれた設計目標・道筋の痕跡。仕様より効率的なコードへ組み替えた箇所も多い。Basic_Design.md は Rust/Wasm・WebGPU・SharedArrayBuffer を中核と記すが、いずれも未実装。",
      "howToApply": "機能の有無は必ずコードを読んで確認する。Doc を根拠に『実装済みのはず』と推論しない。"
    },
    {
      "id": "auth-is-internal-only",
      "severity": "info",
      "title": "AuthGuard は社内配布前提の目隠し",
      "detail": "正解ハッシュがバンドルに含まれ localStorage にも同じ値を保存するため DevTools から突破可能。サーバーを持たない構成では原理的にこれ以上の強度は出せない。社内配布に留める方針で許容済み。",
      "howToApply": "認証強化を提案しない。セキュリティ指摘は不要。"
    },
    {
      "id": "deploy-password-hash",
      "severity": "high",
      "title": "デプロイ前にパスワードハッシュを照合すること",
      "detail": ".env が無い環境ではビルドが AuthGuard.tsx のフォールバックハッシュを使う。本番と異なるハッシュでデプロイすると全員ログインできなくなる。",
      "howToApply": "本番バンドルを取得し 2cfbedf50a09b0767c5d4c17ecb94f4b81fc17a160ef609c0b7566f5fcea2974 が含まれることを確認してから firebase deploy する。"
    },
    {
      "id": "branch-before-commit",
      "severity": "medium",
      "title": "main に直接コミットしない",
      "detail": "運用は『ブランチを切る → コミット → main へ --no-ff マージ → push → デプロイ』。",
      "howToApply": "git checkout -b improve/<topic> から始める。"
    }
  ],
  "domainRules": [
    {
      "id": "white-is-transparent",
      "title": "純白 RGB(255,255,255) は透明 (alpha 0)",
      "detail": "decodeTGA が読み込み時に純白を alpha 0 へ変換する。encodeTGA は保存時に alpha 0 を純白へ戻す。アニメ業界の慣習に合わせた独自仕様。",
      "location": "src/engine/tga.ts"
    },
    {
      "id": "transparent-wall-rule",
      "title": "透明画素は『透明領域を塗るとき』だけ通り抜けられる",
      "detail": "isBoundary は alpha 0 の画素について targetA !== 0 を返す。基準色が不透明 (線をクリックした場合) は透明部分を壁として扱い、セル全面が塗り潰されるのを防ぐ。",
      "location": "src/engine/paintAlgorithm.ts"
    },
    {
      "id": "relative-paths",
      "title": "ファイル識別子はルートからの相対パス",
      "detail": "ファイルツリーを階層表示するため Cat/_go/A0001.tga のような相対パスを使う。getFileHandle() は名前を 1 つしか受け取れないので、resolveFileHandle() でディレクトリを 1 段ずつ辿る。folderHandleA/B にはサブフォルダではなくルートのハンドルを保持する。",
      "location": "src/engine/fileSystemPath.ts"
    },
    {
      "id": "frame-merge",
      "title": "A/B は異名連番。フレーム番号で対応付ける",
      "detail": "Win A と Win B はファイル名が異なる (例: a_0001.tga と b_go0001.tga)。extractFrameNumber でフレーム番号を取り出し buildMergedFrameData でマージする。ビューごとの実ファイル名は resolveFileNameForView(index, view) で解決する。unifiedFileList の中身は A 側の代表名なので、B の保存先や表示に使ってはいけない。",
      "location": "src/store/types.ts, src/store/slices/fileSlice.ts"
    },
    {
      "id": "sync-frame-offset",
      "title": "左右連動はコマ差を保つ",
      "detail": "連動を押した瞬間の splitFileIndex - currentFileIndex を syncFrameOffset として記録し、以降その差を保ったまま追従する。押した時点では両者とも動かない。相手側への追従はストア (setCurrentFileIndex / setSplitFileIndex / stepCell) が担うので、コンポーネント側で両方に同じ番号を入れてはいけない。⚠️ コマ送り (stepCell) で両方に delta を足さないこと。片方が端 (先頭 / 末尾) で止まった時点でコマ差が失われ、戻ってきたときに相手だけ 1 コマずれたまま進む。「Win B で先頭の 0001 だけ出てこない・飛ばされる」という形で現れる (2026-08-29 の報告・修正)。追従する側は必ず『主導する面 (activeViewIndex) + コマ差』から決める。⚠️ 主導する面が端で動けないときは、どちらも動かさないこと。相手だけ動かすとやはりコマ差が変わる。⚠️ syncFrameOffset と実際のズレ (splitFileIndex - currentFileIndex) を食い違わせないこと。連動中にフォルダを開き直したら、Win B の位置は必ず『Win A + コマ差』へ引き直す (linkedSplitIndex)。別々に決めると『連動中 (同じコマ) と出ているのに Win B だけ 1 コマ先』になり、先頭のコマではそれ以上戻れないため、そのズレだけ直せないまま残る (2026-08-29 の報告・修正)。⚠️ 連動していないときに開いた Win B は、Win A が見ているコマから始めること (alignedWithOther)。別々のコマから始まると、そのまま連動を押したときに意図しないコマ差が記録される。そのコマが Win B に無いときだけ、B に実体がある先頭のコマへ落とす (NO DATA で開かないため)。⚠️ 「差を揃える」ボタンは記録上のコマ差だけで出さないこと。実際のズレと食い違ったときに、揃え直す手段が画面から消える。",
      "location": "src/store/slices/viewSlice.ts, src/store/slices/fileSlice.ts (stepCell)"
    },
    {
      "id": "undo-model",
      "title": "履歴は historyStack[historyIndex] が常に表示中の状態",
      "detail": "saveUndoState は操作の直前に呼ばれるため、積んだ枠の中身は commitLiveState() で遅延同期される。履歴は Win A / Win B で独立 (historyStack / splitHistoryStack)。最初の操作時に『編集前』を基準状態として積む。",
      "location": "src/store/slices/documentSlice.ts"
    },
    {
      "id": "readonly-guard",
      "title": "TGA 以外の画像は閲覧専用でツールが効かない",
      "detail": "decodeAnyImageFile は .tga 以外 (JPG/PNG) に isReadOnly: true を付ける。handleMouseDown は右クリックのパン判定より後、ツール分岐より前でこれを弾く。『ツールが反応しない』症状の最有力候補。",
      "location": "src/engine/imageDecode.ts, src/components/panels/CellWindow.tsx"
    },
    {
      "id": "floating-window-transform",
      "title": "独立ウィンドウの位置は element.style.transform へ直接書く",
      "detail": "再描画を挟まないための高速化。React の管理外なので、ドッキングへ戻すときに明示的に消さないとパネルがズレたまま残る。位置・サイズ・重なり順はストア (windowSlice) が保持する。",
      "location": "src/hooks/useFastDraggable.ts, src/hooks/useFloatingWindow.ts"
    },
    {
      "id": "path-root-matches-handle",
      "title": "パスの起点とフォルダハンドルの起点を必ず揃える",
      "detail": "ファイル識別子はルートからの相対パス。resolveFileHandle() はその先頭からディレクトリを 1 段ずつ辿るので、ストアが持つ folderHandleA/B は『そのパスの起点』でなければならない。サブフォルダのハンドルを持たせたまま『_go/A0001.tga』を渡すと『_go の中の _go』を探して必ず失敗する。読み込みは fileMap のフォールバックで通ってしまい、保存だけが落ちるので気づきにくい。フォルダを開く経路は 5 つあるので、増やすときも collectImageFilesRecursively と isSupportedImageFile を必ず通すこと。",
      "location": "src/engine/fileSystemPath.ts, src/components/panels/FileBrowser.tsx, src/components/layout/MenuBar.tsx"
    },
    {
      "id": "frame-map-is-one-to-one",
      "title": "フレーム対応表は 1 ファイル 1 エントリ。逆引きはパスの完全一致で",
      "detail": "buildMergedFrameData はフレーム番号をキーにするが、サブフォルダをまたいで同じ番号があると (_go/a0001.tga と b/b0001.tga) 後勝ちで上書きされ、ファイルが一覧から消える。番号が埋まっている場合はディレクトリを付けて別キーにし、必ず 1 対 1 を保つこと。⚠️ コマ番号はファイル名の部分からだけ取ること (baseNameOf を通す)。パス全体から探すとフォルダ名の数字を拾い、ATO_OP_029_trace/_sheet/cut.tga が『029 コマ目』になって連番の列へ紛れ込む (2026-08-31 の報告・修正)。数字が無いファイルはファイル名をそのまま番号として使う (パスを返すと、A と B で先頭のフォルダ名が違うだけで別コマになる)。⚠️ 番号が埋まっているときの代替キーに、フォルダのフルパスを使わないこと。先頭のフォルダ名は A と B で必ず違う (trace / paint) ため、同じ位置・同じ名前のファイルが永久に対にならない。pathWithoutRoot で先頭を落とした位置で作る。⚠️ B の割り当ては 1) ルートを除いた相対パスが同じ枠 → 2) 同じ番号でフォルダの位置が同じ空き枠 → 3) 同じ番号の空き枠 → 4) B だけの新しい枠、の順。2 を飛ばすと、A 側にサブフォルダが 2 つあるとき 番号を先取りした別フォルダのファイルと対になる。⚠️ 対応づけはまず相対パスの一致で決めること。番号だけで対応づけると、A 側にサブフォルダが 2 つあるとき (カットを丸ごと Win A へ開いた場合など) B のファイルが『番号を先に取った別フォルダの A ファイル』と対になり、Win A と Win B に別のセルが並ぶ (2026-08-31 の報告・修正)。同じ相対パスがあれば同じセルなので、そこを先に見る。⚠️ 並べ替えは『A のフォルダごとにまとめ、その中は自然順』。フォルダは compareNatural、中身も compareNatural で並べ直す (入力順に依存させない)。番号で並べると、番号を先に取った側だけが数値キーになり、取れなかった側はディレクトリ付きキーになって末尾へ回るため、_bg/x0001 → x0002 → x0003 → _go/y0004 → y0005 → _go/y0001 … と、コマ送りの途中で別フォルダへ飛び、あとから戻ってくる並びになる (2026-08-31 の報告・修正)。パスで並べればフォルダごとにまとまり、その中は自然順になり、ツリー (fileListA / fileListB の順) とコマ送りの順が一致する。⚠️ Win B にしか無いコマ (追加リテイク) を B 側のパスで並べないこと。B のフォルダ名しだいで番号と無関係な場所 (先頭など) に現れる。寄せ先は『B の並びで前後にある、両方にあるコマ』の A 側フォルダ → 無ければ B 自身のフォルダが A 側にもあればそこ → それも無ければ最後にまとめる、の順で決め、フォルダの中では番号が収まる位置へ差し込む。ツリーの位置から統合リストの位置を引くときはフレーム番号で当てず indexOfFileForView() を使う。path → index → path が往復で一致しないと、選んだ行と光る行がずれて『選択が飛ぶ・巻き戻る・項目が飛ばされる』症状になる。",
      "location": "src/store/types.ts (buildMergedFrameData), src/store/slices/fileSlice.ts (indexOfFileForView)"
    },
    {
      "id": "rename-is-destructive",
      "title": "リネームは 2 段階。move() は同名を黙って上書きする",
      "detail": "FileSystemFileHandle.move() は既存の同名ファイルを確認なく上書きする。番号を繰り上げる連番リネーム (A0001→A0002, A0002→A0003) を素直に順次実行すると 1 手目で既存の A0002 が消える。名前が入れ替わる計画は一時名を経由する 2 段階で実行すること。衝突・不正な名前は実行前に検出し、1 件も書き換えずに中止する (部分適用を作らない)。",
      "location": "src/engine/renamePlan.ts, src/store/slices/fileSlice.ts (renameFiles)"
    },
    {
      "id": "write-permission-is-not-granted",
      "title": "書き込み許可は既定で付いていない",
      "detail": "showDirectoryPicker() も drag & drop も既定は読み取り許可のみ。そのまま createWritable() を呼ぶと NotAllowedError になる。保存・リネーム・複製・削除の直前に ensureWritePermission() を通すこと。requestPermission() はユーザー操作の直後にしか通らないため、保存ボタンや Ctrl+S から連なる呼び出しの中で使う。",
      "location": "src/engine/fileSystemPath.ts (ensureWritePermission)"
    },
    {
      "id": "save-as-keeps-original-dirty",
      "title": "名前を付けて保存は、元ファイルを未保存のまま残す",
      "detail": "書き出し先は開いているフォルダの連番に含まれないため、保存しても元ファイルの isDirty をクリアしない。フォルダの連番が UI 全体を駆動している以上、保存先を編集対象へ切り替えると表示中のカットごと変わってしまう。2026-08-26 にユーザー判断でこの挙動を仕様として確定した。ユーザーからの再依頼が無い限り、一般的なアプリに合わせる変更を提案しないこと。",
      "location": "src/store/slices/documentSlice.ts"
    },
    {
      "id": "chromium-only-by-design",
      "title": "対象ブラウザは Chromium 系。他では D&D したフォルダを保存できない",
      "detail": "書き込み可能なハンドルを得る getAsFileSystemHandle() が Chromium 系のみ。非対応環境ではwebkitGetAsEntry() で読み込み、保存だけができない扱いにしている。もともと File System Access API 前提の構成であり、2026-08-26 にユーザー判断で仕様として確定した。Firefox / Safari 対応や、そのための警告 UI を提案しないこと。",
      "location": "src/components/panels/FileBrowser.tsx, src/components/panels/CellWindow.tsx"
    },
    {
      "id": "lasso-undo-on-commit",
      "title": "投げ縄の履歴は開始時ではなく確定の直前に積む",
      "detail": "投げ縄 (closedFill / lasso) は塗るかどうかが離した時点まで決まらないため、saveUndoState は handleMouseUp で closedAreaFill を呼ぶ直前に呼ぶ。開始時に積むとクリックしただけで履歴が増え「未保存」になる。またキャンバスの外で離した場合は塗らずに破棄するが、window の pointerup は canvas の onMouseUp より先に来るため、e.target がそのビューの canvas なら何もしないこと (無条件に打ち切ると通常の塗りが消える)。",
      "location": "src/components/panels/CellWindow.tsx"
    },
    {
      "id": "lighttable-win-a-only",
      "title": "ライトテーブル / オニオンスキンは Win A 専用",
      "detail": "renderCanvasInstance の描画は isLeft && lightTable.enabled と isLeft && lightTable.items.length > 0 で左キャンバスに限定している。2026-08-26 にユーザー判断で仕様として確定した。Win B へ広げる変更や、アクティブな側へ出す変更を提案しないこと。",
      "location": "src/components/panels/CellWindow.tsx"
    },
    {
      "id": "cut-folder-scan-single-source",
      "title": "カットフォルダの走査は scanCutRootFolder に一本化する",
      "detail": "「メニューバー > フォルダを開く」と「ファイルブラウザ > カットフォルダを開く」は src/engine/cutFolder.ts の scanCutRootFolder を通す。自前で走査を書かないこと。以前は 2 箇所に重複しており、(1) サブフォルダが無いフォルダで片方だけ setFolderHandleA を呼んで前のカットのサブフォルダ一覧が残る (2) 画像フォルダ判定に .tga と .png だけの独自正規表現を使い .jpg が漏れる、という食い違いが出ていた。サブフォルダが無い場合も (Root) 1 件として setCutRootFolder を通すこと。",
      "location": "src/engine/cutFolder.ts"
    },
    {
      "id": "encode-restores-white",
      "title": "encodeTGA は alpha 0 の画素を純白 RGB として書き戻す",
      "detail": "「RGB(255,255,255) は完全透明 / (254,254,254) は不透明な白」という PaintMan の規約に合わせ、保存時は alpha 0 の画素を RGB(255,255,255) として書く。戻すのは RGB であってアルファではない。消しゴム (paintAlgorithm.ts の drawCircle) は alpha を 0 にするだけで RGB を触らないため、ここで白に戻さないと消した部分が元の色で復活する。src/engine/tga.test.ts が往復を固定している。",
      "location": "src/engine/tga.ts"
    },
    {
      "id": "listener-capture-must-match",
      "title": "removeEventListener は addEventListener と capture の指定を一致させる",
      "detail": "capture: true で登録したリスナーは、capture を指定しない removeEventListener では外れない。ドラッグ系の後片付けは早期 return より前に、必ず実行すること。残ったハンドラが次のドラッグのイベントを先に拾ってフラグを倒すと、本来のハンドラが早期 return して他のリスナーまで外れなくなる。",
      "location": "src/hooks/useResizableWindow.ts"
    },
    {
      "id": "roll-native-playback-only",
      "title": "ロール再生はブラウザのネイティブデコーダに任せる",
      "detail": "デコードを自前でやらないことがこの機能の設計の要。ハードウェア再生になり、バンドルもメモリも増えない (追加は約 14KB)。ffmpeg.wasm は約 30MB 増で CPU デコードのため、2026-08-26 にユーザー判断で「積まない」と決めている。再生できないコーデックはコーデック名と ffmpeg の変換コマンドを出して変換を促す。ffmpeg.wasm やブラウザ内変換を提案しないこと。",
      "location": "src/engine/videoSource.ts, src/store/slices/rollSlice.ts"
    },
    {
      "id": "roll-mime-and-codecs",
      "title": ".mov は MIME を付け替える。ProRes は原理的に再生できない",
      "detail": "実測 (2026-08-26 / 本番の Chrome): video/quicktime は canPlayType が空を返すため、File をそのまま <video> に渡しても再生できない。file.slice(0, file.size, \"video/mp4\") でデータをコピーせず MIME だけ差し替えること (new Blob([file]) は全体をメモリへ載せる)。H.264 / VP9 / AV1 は再生可。⚠️ 付け替えるのは .mov (video/quicktime) だけ。WebM まで video/mp4 と名乗らせると中身と食い違ってデコードできず「再生できません」になる (2026-08-29 に実測・修正)。ProRes (apcn/apch/ap4h 等)・DNxHD・非圧縮は <video>・MSE・WebCodecs のいずれも非対応。HEVC は環境依存。",
      "location": "src/engine/videoSource.ts"
    },
    {
      "id": "roll-drop-routing",
      "title": "セルの窓に落とされた動画はロールウィンドウへ回す",
      "detail": "Win A / Win B の D&D は画像フォルダ用だが、撮影ロールをそこへ落とすのは自然な操作で、しかもロールウィンドウを閉じていると落とす先が他に無い。handleFolderOrFilesNativeDrop は画像が 1 枚も無かったとき、dataTransfer.files に動画があれば loadRollFile へ回す (loadRollFile が窓も開く)。ここを「画像が見つかりません」で突き放さないこと。",
      "location": "src/components/panels/CellWindow.tsx"
    },
    {
      "id": "plain-folder-keeps-winb",
      "title": "単独フォルダを開くときは setCutRootFolder を通さない",
      "detail": "setCutRootFolder は「カットを開き直す」操作で、A と B の両方を組み直す。サブフォルダが 1 つしか無いと defaultDirB が決まらず fileListB が空になるため、片側だけ開き直したいときに使うと Win B の中身が消え、2 画面 + 連動が使えなくなる。サブフォルダを持たないフォルダは openPlainFolderAsA を使うこと (Win B に触れず、前のカットのサブフォルダ一覧だけ捨てる)。なお setFolderHandleA/B はリネーム・複製・削除後の一覧更新にも使われているので、あちらにカット状態を捨てる処理を足してはいけない。",
      "location": "src/store/slices/fileSlice.ts, src/components/layout/MenuBar.tsx"
    },
    {
      "id": "natural-file-order",
      "title": "ファイル一覧の並べ替えは sortNatural を通す",
      "detail": "素の Array.prototype.sort() を使わないこと。辞書順になるため、ゼロ埋めされていない連番が a1, a10, a11, a2 … と並ぶ。src/engine/naturalOrder.ts の sortNatural / compareNatural (Intl.Collator の numeric: true) を使う。照合器は使い回すこと — localeCompare を都度呼ぶと数千コマのカットで遅くなる。ツリーは fileListA / fileListB の順、連番表は unifiedFileList の順を使うので、両方を同じ基準で並べないと表示が食い違う。",
      "location": "src/engine/naturalOrder.ts"
    },
    {
      "id": "dock-tab-on-right-edge",
      "title": "縦帯の跡地では、戻すタブを右端に縦向きで置く",
      "detail": "参照ウィンドウ・ロールウィンドウを切り離したときの跡地 (DockPlaceholder の strip-v) は、ドロップ先のタブを帯の右端に縦向きで置く。2026-08-27 にユーザーの指定で上端から変更した。上端に横向きで置くと、編集エリアの右隣にある帯の中で本体から遠く、タブを持って行きにくい。上端へ戻す変更を提案しないこと。",
      "location": "src/components/common/DockPlaceholder.tsx"
    },
    {
      "id": "no-header-save-button",
      "title": "ヘッダーに保存ボタンを置かない",
      "detail": "保存はメニューの「上書き保存」と Ctrl+S に一本化してある。2026-08-27 にユーザーの指定でヘッダーの保存ボタン (未保存インジケーター兼用) を外した。未保存かどうかは Win A / Win B のタイトルバーの * で分かる。ヘッダーへの保存ボタンや未保存バッジの追加を提案しないこと。",
      "location": "src/components/layout/MenuBar.tsx"
    },
    {
      "id": "drop-handles-need-timeout",
      "title": "getAsFileSystemHandle() は期限を切って待つ",
      "detail": "ドロップされた項目の FileSystemHandle は resolveDropHandles を通して集めること。getAsFileSystemHandle() は解決しないまま返ってこないことがあり (ヘッドレス Chrome で実測)、Promise.all で素直に待つとドロップ処理ごと止まって「落としても何も起きない」状態になる。期限内に取れなければ webkitGetAsEntry() の読み取り専用エントリへ落ちればよい。また動画の判定は dataTransfer.files だけで行わないこと。フォルダを落とすとそこにはフォルダ自体しか入らないので、findDroppedVideoFile で中を探す。",
      "location": "src/engine/fileSystemPath.ts, src/engine/videoSource.ts"
    },
    {
      "id": "pane-layout-owns-order",
      "title": "面の並び順を JSX に書かない",
      "detail": "どの面をどこへ、どう重ねて出すかは engine/paneLayout.ts のモデルと paneLayout ステートが持つ。CellWindow は slotsToRender を回して描くだけで、面の順序を JSX に書いてはいけない。書くと入れ替え・一面表示・タブ重ねができなくなる (2026-08-27 以前がその状態だった)。⚠️ 「その面を開いているか」は従来どおり isSplitView / referenceCanvas.isOpen / roll.isOpen が持つ。連動やファイル読み込みから参照されているため移していない。CellWindow の syncPaneVisibility が一方向に流し込んでレイアウトを追従させる。タブの × もこれらのフラグを動かすこと。⚠️ タブの D&D には PANE_DRAG_TYPE を付ける。付けないとフォルダの D&D と区別できず、タブを運んだだけで「画像ファイルが見つかりませんでした」が出る。",
      "location": "src/engine/paneLayout.ts, src/components/panels/PaneTabBar.tsx, src/components/panels/CellWindow.tsx"
    },
    {
      "id": "roll-arrow-keys",
      "title": "ロールのキーは ← → ↑ ↓ と Space。効き先は最後に触った面で決まる",
      "detail": "← → で 1 コマ、Shift + ← → で 1 秒ぶん送り戻し、↑ ↓ で前後のロール、Space で再生 / 一時停止。⚠️ 2 面開いているときは、キーの操作はすべて 2 面いっしょに効かせること (2026-08-29 のユーザー指定)。コマ送りも同様で、再生の連動 (roll.sync) が入っていれば時刻差を保ったまま (syncPartnerTime)、入っていなければ相手も今の位置から同じコマ数だけ動かす (stepPartner)。両方から書かないこと — 連動中に stepPartner まで動かすと、保っていた時刻差が崩れる。⚠️ 相手のコマ数は相手の fps で秒へ直すこと。24fps と 30fps を並べたとき、同じコマ数で送ると「1 秒」がずれる。⚠️ 片方だけ動かす道を残すこと。面の ◀ ▶ ボタンはその面だけを送る (stepSelf)。左右のずれを作ってから連動させる使い方があるため。⚠️ ← → ↑ ↓ Space はいずれもセル側と取り合いになる (↑ ↓ = コマ送り、Space = 押しながらパン、← → = ツリーのフォルダ開閉)。2026-08-29 にユーザーの指定でロールにも割り当てたので、どのキーも activeSurface (最後に触った面) が 'roll' のときだけ拾うこと。⚠️ 拾ったら必ず stopPropagation すること。RollViewer の登録は window の capture で、useGlobalShortcuts (document の capture) と CellWindow (window の bubble) より先に走る。止めないとセルのコマ送りやパンまで同時に動く。⚠️ ボタンにフォーカスがあるときは Space を横取りしないこと。ブラウザが keyup でクリックにするため、再生と停止が 1 回ずつ走って何も起きないように見える。⚠️ キーを拾うのは 1 面だけ (アクティブな面。閉じていればもう一方)。両方が拾うと二重に進む。⚠️ ハンドラの登録条件を「再生できること」にしないこと。コーデック非対応で止まった面からでも ↑ ↓ で次のロールへ移れる必要がある。⚠️ 1 秒送りは秒数を直接足さず、fps ぶんのコマを進めること。秒で足すとコマの境界からずれ、そのあとのコマ送りが半コマずれた位置を行き来する。⚠️ 入力欄 (シークバー・fps の選択) にフォーカスがあるときは横取りしないこと。それらは左右キーで操作するものなので、奪うとつまみが動かせなくなる。",
      "location": "src/components/panels/RollViewer.tsx"
    },
    {
      "id": "roll-folder-list",
      "title": "ロールはフォルダ単位で一覧を持つ",
      "detail": "1 つのフォルダに複数のロールを入れて順に確認する運用があるため、ドロップされたフォルダからは collectDroppedVideoFiles で全部集め、面ごとの roll.views[id].files に持つ。並びは「浅い階層が先、その中は自然順」で、1 本だけ開くときの先頭もこの順序で決まる。⚠️ 一覧は面ごとに持つ (2026-08-29 時点)。ツリーも面ごとに 1 本ずつ並べるので、共有すると「ロール A に落としたフォルダが B のツリーにも出る」ことになる。ただし 2 面目を開くときだけは、一覧が空なら相手のものを引き継ぐ (openRollWindow)。⚠️ ロールの一覧は FileBrowser の「撮影ロール」区画に、面ごとに 1 本ずつ並べる (RollFileTree という別コンポーネントは無い)。表示は TreeItemNode をフィルムのアイコンで使い回すが、コマ番号・複数選択・リネーム/削除の右クリックはロールへ渡さないこと。",
      "location": "src/engine/videoSource.ts, src/components/panels/FileBrowser.tsx"
    },
    {
      "id": "tree-dispatches-by-extension",
      "title": "ファイルツリーが入口。拡張子で表示先を決める",
      "detail": "2026-08-27 に「窓の型に合わせてファイルを落とす」から「どこへ落としてもフォルダを開き、ツリーから開いたファイルの拡張子で表示先が決まる」へ変えた。ドロップは App の一番外側で受ける (Win A / Win B / ロールの窓は自前の処理で stopPropagation しているので、そこへ落とした分はここへ来ない)。走査は engine/dropFolder.ts の readDroppedFolder に一本化すること。⚠️ 映像を fileListA / unifiedFileList へ混ぜないこと。あちらはフレーム番号で対応づける連番の器で、映像名が混ざるとコマの対応が壊れる。表示用の並びとして後ろに足し、位置が連番の長さを超えたら映像として扱う。セル画像は activeViewIndex の側 (Win A / Win B) へ出す。",
      "location": "src/App.tsx, src/engine/dropFolder.ts, src/components/panels/FileBrowser.tsx"
    },
    {
      "id": "roll-two-panes-and-sync",
      "title": "ロールは 2 面。連動はアクティブな面が主導する",
      "detail": "ロールは rollA / rollB の 2 面あり、修正前と修正後を並べて見比べられる。⚠️ 連動は 2 種類ある。sync は再生 (同じロールの中の時刻)、fileSync はツリーの選択 (一覧の何本目を開くか)。混ぜないこと。⚠️ 映像の一覧は面ごと (roll.views[id].files) に持ち、ツリーも面ごとに 1 本ずつ並ぶ。⚠️ 連動中のずれ直しは、操作した側 (roll.activeId) だけが行うこと。両方が相手を直すと互いにシークをかけ合い、再生が始まらなくなる (2026-08-27 に実際に踏んだ)。⚠️ 連動開始時の時刻差 (syncOffset) を保つこと。片方を頭出ししてから連動させる使い方があり、強制的に同じ時刻へ合わせると狙って選んだ位置がずれる (セルの左右連動と同じ考え方)。⚠️ <video> を登録する ref のコールバックは useCallback で固定すること。毎レンダー作り直すと React が null → 要素 の順で呼び直し、その一瞬だけ登録が外れる。",
      "location": "src/store/slices/rollSlice.ts, src/components/panels/RollViewer.tsx, src/components/panels/rollVideoRegistry.ts"
    },
    {
      "id": "empty-start-open-by-content",
      "title": "起動時は何も出さない。開いた内容がウィンドウを決める",
      "detail": "⚠️ createDefaultLayout に面を置かないこと。Win A を最初から出しておくと、空のまま場所を取り続けて他の面を並べられない (2026-08-27 の報告)。⚠️ hidePane に「最後の 1 枚は残す」規則を入れないこと。残すと居座った面が消せなくなり、同じ状態に戻る。全部閉じたら作業領域は空の案内になる。セルを開けば Win A、映像を選べばロールが出る。ただしカットやフォルダを明示的に開いたときは、隠していても Win A を出す(setCutRootFolder / openPlainFolderAsA / 画面全体のドロップ)。開いたのに何も起きないように見えるのを避けるため。",
      "location": "src/engine/paneLayout.ts, src/store/slices/viewSlice.ts, src/components/panels/CellWindow.tsx"
    },
    {
      "id": "roll-split-affordance",
      "title": "ロールの 2 画面はヘッダーのボタンから。開いた側が「選択先」になる",
      "detail": "ロールのヘッダーにある 2 画面ボタンで、もう一方の面を開く (Win A / Win B の「左右に並べる」に相当)。⚠️ openRollWindow は開いた面を activeId にすること。開いた直後にツリーで映像を選ぶとその面へ入る、という流れが自然につながる。2 面あるときは、ツリーの選択先になっている面のヘッダーに「選択先」を出す。⚠️ 仕組みだけ入れて導線を出さないと「1 面しか開けない」と受け取られる (2026-08-27 の報告)。",
      "location": "src/components/panels/RollViewer.tsx"
    },
    {
      "id": "roll-tree-file-sync",
      "title": "ロール 2 面のツリーの選択も連動できる (再生の連動とは別)",
      "detail": "2 面のツリーで「どのロールを開くか」を連動させる。ファイルブラウザの「撮影ロール」区画にある『選択連動』ボタン (roll.fileSync)。⚠️ 再生の連動 (roll.sync / ロール窓ヘッダーの鎖アイコン) と別物として持つこと。あちらは同じロールの中の時刻を合わせるもので、ロールを跨いだ乗り換えは扱わない。⚠️ 連動を押した時点の一覧上のずれ (B − A、本数) を fileSyncOffset に覚え、以降その差を保つ。押した瞬間には動かさない (セルの左右連動・再生の連動と同じ考え方)。⚠️ 端では切り詰めるだけで fileSyncOffset を書き換えないこと。書き換えると一覧の端で一度止まっただけで狙って付けたずれが失われる。⚠️ 相手が既にそのロールを開いているなら何もしないこと。openedView は blob URL を作り直すので、通すたびに相手の再生が頭へ戻る。⚠️ 追従はストア (selectRollFile → withSyncedPartner) が担う。コンポーネント側で 2 面ぶん selectRollFile を呼んではいけない (ずれが消える)。⚠️ 面を閉じたら fileSync も解くこと。一覧ごと捨てるため、残すと次に開いたとき前のずれで勝手に動く。⚠️ 押せる条件に isOpen を使わないこと。フォルダを落としただけでまだ開いていない面もツリーには並び、そこから選べば開く。条件は「両面に一覧があること」。",
      "location": "src/store/slices/rollSlice.ts, src/components/panels/FileBrowser.tsx"
    },
    {
      "id": "active-surface-key-routing",
      "title": "↑ ↓ と Space の効き先は「最後に触った面」(activeSurface)",
      "detail": "セルとロールで同じキーが別の意味を持つ (セル: コマ送り / 押しながらパン、ロール: 前後のロール / 再生)。どちらへ効かせるかは uiSlice の activeSurface ('cell' | 'roll') で決める。⚠️ コンポーネントの state で持たないこと。キーを拾うのは window のハンドラで、そこからは React の状態が見えない。⚠️ 切り替えはストア側で行う。ロールを開く・ツリーでロールを選ぶ・ロールの窓を触る (setActiveRollId) で 'roll'、セルのフォルダを開く (setFolderHandleA/B・setFolderFilesA/B・setCustomDropFolderA/B・setCutRootFolder・openPlainFolderAsA・setSelectedSubDirA/B)・セルのコマを選ぶ (setCurrentFileIndex / setSplitFileIndex)・セルの窓を触る (setActiveViewIndex)・2 画面に並べる (toggleIsSplitView) で 'cell'。ロールが 1 面も残らなくなったら 'cell' へ戻す。⚠️ 「開いた」も操作に含めること。ロールを開いたまま TGA フォルダを開くと ↑ ↓ がロールに効いたままになり、セルが動かない (2026-08-29 の報告)。⚠️ 表示中のコマを選び直したときも 'cell' へ戻すこと。setCurrentFileIndex / setSplitFileIndex は同じ位置なら早期 return するので、そこで取りこぼす。⚠️ ← → もロール側が無条件に拾わないこと。セルのコマ送りには使わないが、ファイルツリーではフォルダの開閉に使う。ロールを開いている間だけツリーが開け閉めできなくなる (2026-08-29 の報告)。",
      "location": "src/store/slices/uiSlice.ts, src/store/slices/rollSlice.ts, src/components/panels/RollViewer.tsx"
    },
    {
      "id": "debug-log-outside-store",
      "title": "操作ログはストアの外に置く。失敗しても alert を出さない",
      "detail": "DEBUG ウィンドウ (右サイドバー最下段) の操作ログは engine/debugLog.ts が持ち、DebugLogPanel だけが useSyncExternalStore で購読する。⚠️ ログをストア (zustand) へ入れないこと。1 行足すたびにストアが更新され、購読している全パネルが再描画される。⚠️ 毎コマ走る場所 (ロールの requestVideoFrameCallback、キャンバスの描画ループ) から logDebug を呼ばないこと。ログが埋まって肝心の操作が流れる。⚠️ 記録には必ず数字を入れる (枚数・統合コマ数・A と B の表示位置・コマ差)。報告と突き合わせるとき、対応づけの食い違いか位置の食い違いかをその場で切り分けられる。⚠️ 上限 500 行で古いものから捨てる (開きっぱなしで使うため)。⚠️ 全文コピーが失敗しても alert を出さないこと。ダイアログはページの実行を止め、ログを見ながら操作を続けられなくなる (2026-08-31 のヘッドレス検証で実際に踏んだ)。ボタンの表示で知らせ、本文はコンソールへ出して手で拾えるようにする。⚠️ navigator.clipboard は権限やフォーカスしだいで解決も拒否もしないことがある。期限を切って execCommand の経路へ落とすこと。⚠️ 2 画面連動の追跡に振り切ってあること。連動を入れた時点で対にした 2 枚 (位置とファイル名)、どのキー・どのボタンから動いたか (source)、Win A と Win B のそれぞれの移動前後 (位置とファイル名) を 1 行に収める。パネルには「連動のみ」の絞り込みと、今の状態 (2 画面 / 連動 / コマ差 / A・B の位置とファイル名) を出す帯を置く。⚠️ 食い違いの判定は isSyncPairConsistent (types.ts) を使うこと。「B === A + コマ差」だけで見ると、端で追従側が切り詰められた並び (全 4 コマ・コマ差 +1 で B が先頭に来ると A=0 / B=0) を誤って食い違いと呼ぶ。どちらが主導でも辻褄が合えば正しい。⚠️ 再生の自動送りは PLAYBACK_SOURCE を渡して記録しないこと (毎コマ走る)。⚠️ 表示倍率 (view) も記録すること。「コマを送ったら倍率が変わった」の切り分けには、セルの移動と倍率の変化が並んでいる必要がある (「連動のみ」の絞り込みにも view を含める)。⚠️ 倍率は毎フレーム書かないこと。ホイールは前に記録した倍率から 1 割以上動いたときだけ、パンのドラッグは記録しない (倍率が変わらないため)。",
      "location": "src/engine/debugLog.ts, src/components/panels/DebugLogPanel.tsx"
    },
    {
      "id": "auto-fit-keeps-user-zoom",
      "title": "自動フィットはユーザーが決めた倍率を壊さない",
      "detail": "セルを開いたとき、表示領域の高さに合わせて倍率を自動で決める (CellWindow の fitToScreenHeight)。⚠️ 走らせるのは『画像のサイズが変わったとき』かつ『倍率が自動で合わせた値のまま (lastFitTransformRef と一致) のとき』だけ。以前はサイズが変わるたびに合わせ直していたため、設定シート (_sheet/cut.tga) のようにサイズの違うファイルを 1 枚挟むだけで、拡大して塗っていた倍率が飛んでいた (2026-08-31 の報告・修正)。⚠️ 同じサイズのコマを送っている間は触らないこと (これは 2026-08 以前からの決まり)。⚠️ 自動フィットは Win A の画像サイズで決め、Win A / Win B の両方へ適用する。⚠️ 倍率が変わる操作は DEBUG ログの view カテゴリへ、変更前後と理由 (自動フィット / ホイール / 等倍に戻す / ウィンドウのリサイズ) を残すこと。",
      "location": "src/components/panels/CellWindow.tsx, src/store/slices/uiSlice.ts"
    }
  ],
  "workflow": {
    "commands": {
      "dev": "npm run dev",
      "test": "npm test  # 155 件",
      "typecheckWithTests": "npm run typecheck",
      "build": "npm run build",
      "deploy": "npm run build && firebase deploy --only hosting --project kingfisher-paint-2026"
    },
    "beforeCommit": [
      "npx tsc --noEmit",
      "npm test",
      "npm run build"
    ],
    "conventions": [
      "回答は日本語で行う",
      "検証した事実と推測を明確に分けて書く。推測で断定しない",
      "原因が確定できないときは、ユーザーがツール無しで判定できる方法を提示する (カーソルの形・保存ボタンの色・履歴パネルの増減など)",
      "修正後は tsc / test / build を必ず通す",
      "意図的にコードを壊してテストが落ちることを確認する検証が有効",
      "修正のたびにマージで止めず、デプロイと GitHub への push まで行う (2026-08-26 ユーザー指示)"
    ],
    "browserSmokeTest": {
      "why": "ファイル選択ダイアログは自動化できないが、それ以外は実ブラウザで確認できる。「実機でしか確認できない」と諦める前にここを使うこと。",
      "how": [
        "Node 22 には WebSocket が内蔵されているので、追加の依存なしで CDP を叩ける",
        "chrome.exe --headless=new --remote-debugging-port=9222 --user-data-dir=<一時ディレクトリ> about:blank",
        "http://127.0.0.1:9222/json/list から page ターゲットの webSocketDebuggerUrl を取る",
        "Runtime.enable / Page.enable のあと Page.navigate、localStorage に 'kingfisher_auth_token_v1' = パスワードハッシュ を入れて再読み込みすると AuthGuard を通過できる",
        "Runtime.evaluate で DOM を調べ、Input.dispatchKeyEvent でショートカットを叩ける",
        "Runtime.exceptionThrown と Runtime.consoleAPICalled を拾えばコンソールのエラーも検出できる",
        "動画が要るテストは MediaRecorder で作れる。canvas.captureStream(24) を録画すれば本物の H.264 mp4 が数十 KB で得られる (ffmpeg 不要)",
        "同じバイト列を .mov 拡張子で複製すれば、.mov の MIME 付け替え経路もそのまま検証できる",
        "DOM.setFileInputFiles で隠し file input へファイルを流し込める",
        "⚠️ Runtime.evaluate からの video.play() は NotAllowedError で弾かれる。再生の検証は Input.dispatchMouseEvent で本物のクリックを送ること",
        "⚠️ vite preview は既定で localhost(IPv6) にしか繋がらない。--host 127.0.0.1 を付ける",
        "⚠️ DOM.setFileInputFiles を使うと、その後キーボード入力がページへ届かなくなる (文字キーも含む)。キーを試すテストでは D&D で読み込ませること",
        "⚠️ Input.dispatchKeyEvent が届かない場面がある。ハンドラが信頼済みイベントを要求しない (e.key / e.shiftKey を見るだけ) なら、ページ内から new KeyboardEvent を dispatch すれば経路を通せる。再生など信頼済み操作が要るものだけ Input.dispatchMouseEvent を使う",
        "⚠️ 再生の検証は --autoplay-policy=no-user-gesture-required を付けて起動すると素直に書ける",
        "⚠️ innerText で要素を探すと、同じ文字列で始まる祖先が先に見つかる。ウィンドウの外枠を取りたいときは find の結果をそのまま使う (親を辿らない)"
      ],
      "cannotCover": "showDirectoryPicker / showSaveFilePicker のネイティブダイアログ。フォルダを開く・保存・リネーム・削除の実操作だけは人手が要る。"
    }
  },
  "knownFixedBugs": [
    {
      "symptom": "Ctrl+S が効かない",
      "cause": "存在しない DOM 要素をクリックしようとしていた"
    },
    {
      "symptom": "Win B の保存が別ファイルに書かれる",
      "cause": "保存先に A 側の代表名を使っていた"
    },
    {
      "symptom": "Win B の Undo が効かない",
      "cause": "splitImage がコンポーネントのローカル state だった"
    },
    {
      "symptom": "Ctrl+Z が 2 手戻る / 1 手目が戻せない",
      "cause": "履歴の基準状態が無く undo の条件がずれていた"
    },
    {
      "symptom": "コマ送りで編集が消える",
      "cause": "未保存の確認が無かった"
    },
    {
      "symptom": "隙間閉じが効かない",
      "cause": "UI から値は渡るが floodFill が参照していなかった"
    },
    {
      "symptom": "線をクリックすると全面が塗り潰される",
      "cause": "透明画素を常に通り抜け可能にしていた"
    },
    {
      "symptom": "ドッキングに戻すとパネルがズレる",
      "cause": "直接書いた transform を消していなかった"
    },
    {
      "symptom": "D&D が点滅して挿入できない",
      "cause": "ハイライト時に flex-1 が外れて要素が縮んでいた"
    },
    {
      "symptom": "D&D で 101 枚目以降が欠落",
      "cause": "readEntries() の 100 件制限を繰り返していなかった"
    },
    {
      "symptom": "D&D で複数選択が取りこぼされる",
      "cause": "await を挟んだ後に dataTransfer.items を読んでいた"
    },
    {
      "symptom": "コマ送りでズームが戻る",
      "cause": "画像が変わるたびに自動フィットしていた"
    },
    {
      "symptom": "ファイルツリーがフラットになる",
      "cause": "フォルダを開く経路が相対パスを持たず 1 階層しか走査していなかった"
    },
    {
      "symptom": "Win B のファイルツリーが出ない",
      "cause": "統合リスト (A 側の代表名) から 1 本だけ組み立てていた"
    },
    {
      "symptom": "Win B に画像が出ない (ツリーには並ぶ)",
      "cause": "フォルダを開いた直後の表示コマを無条件で 0 にしていた。B の実体が無いコマを指していた"
    },
    {
      "symptom": "Win B のツリーで選んでも移動しない",
      "cause": "ツリーを 2 本出す条件が『A と B の両方にファイルがある』で、片側だけのとき統合ツリー 1 本になり Win A しか動かせなかった"
    },
    {
      "symptom": "Win B を触ると Win A が反応したように見える",
      "cause": "閲覧専用の通知が単一の真偽値で両ウィンドウに出ていた。独立ウィンドウの枠線も activeViewIndex を反映していなかった"
    },
    {
      "symptom": "サブフォルダの画像がツリーに出ない",
      "cause": "Open A/B が直下 1 階層しか走査していなかった"
    },
    {
      "symptom": ".png が経路によって見えたり見えなかったりする",
      "cause": "拡張子の判定が 5 箇所に散っていた (.tga のみ / .tga と .jpg だけ)"
    },
    {
      "symptom": "階層のあるカットで保存だけ失敗する",
      "cause": "folderHandle にサブフォルダのハンドルを持たせていた (パスの起点と不一致)"
    },
    {
      "symptom": "保存したのに古い画像が出る",
      "cause": "開いた時点の File スナップショットが fileMap に残り、フォールバックで返っていた"
    },
    {
      "symptom": "別フォルダの同名コマが消える",
      "cause": "<input webkitdirectory> のキーがファイル名だけだった"
    },
    {
      "symptom": "名前を付けて保存が元ファイルを上書きする",
      "cause": "メニューもショートカットも上書き保存と同じハンドラに繋がっていた"
    },
    {
      "symptom": "ツリーの選択が先頭へ巻き戻る / 別フォルダへ飛ぶ / 項目が飛ばされる",
      "cause": "フレーム番号での逆引きが 1 対 1 でなく、対応が取れないと無関係な行にフォールバックしていた"
    },
    {
      "symptom": "サブフォルダをまたいで選んだファイルのリネームが衝突扱いで止まる",
      "cause": "衝突判定をファイル名だけで見ていた。_go と _ao に同じコマ番号を振る通常の操作が duplicate / exists と誤判定されていた"
    },
    {
      "symptom": "Win B で投げ縄を引くと輪郭線が Win A に出る",
      "cause": "投げ縄の状態にビューの区別が無く、プレビューの描画が左キャンバス固定だった。塗り自体は Win B に入る"
    },
    {
      "symptom": "投げ縄をキャンバスの外で離すと、ボタンを押していないのに輪郭線がカーソルに付いてくる",
      "cause": "ドラッグ終了の window リスナーが isLassoing を落としておらず、依存配列も [isPanning, isBrushing] で投げ縄だけの操作では登録すらされていなかった"
    },
    {
      "symptom": "投げ縄ツールでクリックしただけ (ドラッグ無し) でも履歴が 1 つ増えて「未保存」になる",
      "cause": "saveUndoState を投げ縄の開始時に呼んでいた。塗るかどうかは離すまで決まらないので、確定の直前に呼ぶ"
    },
    {
      "symptom": "メニューバーからサブフォルダの無いフォルダを開くと、前のカットのサブフォルダ一覧がドロップダウンに残る",
      "cause": "その経路だけ setFolderHandleA で済ませていた。setFolderHandleA は rootFolderName / availableSubDirectories / selectedSubDirA を触らない。フォルダ走査が 2 箇所に重複していたのが元"
    },
    {
      "symptom": "消しゴムで消した部分が、保存して開き直すと元の色で復活する",
      "cause": "encodeTGA が「alpha 0 を白へ戻す」処理を RGB ではなくアルファチャンネルに書いていた。消しゴム (drawCircle) は alpha を 0 にするだけで RGB を残すため、元の色の不透明な画素として保存されていた"
    },
    {
      "symptom": "線画分離で作った 3 枚のレイヤーが、表示切替や削除でまとめて反応する",
      "cause": "addLayer の ID が Date.now() で、同じミリ秒に 3 回呼ばれて同一 ID になっていた"
    },
    {
      "symptom": "Ctrl+1 でブラウザのタブが切り替わると同時にパレットのタブまで変わる",
      "cause": "ショートカットの 1 / 2 / 3 とコマ送りキーに修飾キーの判定が無かった"
    },
    {
      "symptom": "kingfisher_dead_files がほぼ全ファイルを「参照ゼロ」と報告する",
      "cause": "Windows の relative() が返すバックスラッシュ区切りを split('/') で分割しており、ファイル名が取れず正規表現が何にも一致していなかった。path.basename に変更"
    },
    {
      "symptom": "フォルダを開かずに再生を押すと、以降どのコマも表示できなくなる",
      "cause": "空リストのまま (currentFileIndex + 1) % 0 を取って NaN になっていた。NaN === NaN が false のため毎 tick で set が通り、履歴を消し続けたまま復帰できなかった"
    },
    {
      "symptom": "再生中に FPS スライダーを動かしても速度が変わらない",
      "cause": "setInterval の間隔は生成時にしか決まらないのに、fps が useEffect の依存配列に入っていなかった"
    },
    {
      "symptom": "独立ウィンドウをリサイズするたびに window のリスナーが溜まっていく",
      "cause": "capture: true で登録した pointerup / pointercancel を capture 指定なしで removeEventListener していた。残ったハンドラが次のリサイズの pointerup を先に拾って isResizing を倒すため、本来のハンドラが早期 return し pointermove も外れなくなっていた"
    },
    {
      "symptom": "Ctrl+Alt を押していると、どのキーを打っても右パネルが開閉する",
      "cause": "押されたキーを一切見ずに修飾キーだけで判定していた。キーリピートでも連打され、入力欄で打っている最中にも反応していた"
    },
    {
      "symptom": "2 画面で開いて連動を押しても Win B が動かない (Win B が空になっている)",
      "cause": "メニューの「フォルダを開く」がサブフォルダの無いフォルダも setCutRootFolder に流していた。setCutRootFolder は A と B の両方を組み直すので、サブフォルダが (Root) 1 つだけだと fileListB を空にする。openPlainFolderAsA を新設して Win B に触れない経路にした"
    },
    {
      "symptom": "ファイルツリーの並びが連番順にならない (a1, a10, a11, a2, a3 … のように 1 の次が 10 になる)",
      "cause": "ファイル一覧を素の Array.prototype.sort() で並べており辞書順になっていた。連番表 (unifiedFileList) はフレーム番号で数値順に並ぶため、ツリーだけ並びが食い違って見えていた。ゼロ埋めが揃っていれば表面化しないが、撮影・仕上げのデータでは揃っていないことが普通にある。Win A と Win B の両方を読み込んだとき (ツリーが 2 本になるとき) に出る"
    },
    {
      "symptom": "ロールの入ったフォルダを D&D すると「画像ファイルが見つかりませんでした」になる",
      "cause": "動画の判定を dataTransfer.files だけで行っていた。フォルダを落とすとそこにはフォルダ自体しか入らず、中の .mov / .mp4 が見えない。ハンドルとエントリからフォルダの中を探すようにした"
    },
    {
      "symptom": "フォルダを D&D しても何も起きず、エラーも出ない",
      "cause": "getAsFileSystemHandle() が解決しないことがあり (ヘッドレス Chrome で実測)、Promise.all で待っているドロップ処理ごと停止していた。期限を切って読み取り専用のエントリ経路へ落ちるようにした"
    },
    {
      "symptom": "空の Win A が場所を取って、ロールを 2 面並べられない",
      "cause": "closePane が Win A だけ閉じさせず、syncPaneVisibility も winA: true を固定で渡していた。isWinAVisible を追加して閉じられるようにした"
    },
    {
      "symptom": "Win A が画面に固定され、消せないので他のウィンドウを並べられない",
      "cause": "createDefaultLayout が起動時に Win A を置き、さらに hidePane が「最後の 1 枚は残す」規則だったため、Win A だけのときに閉じられなかった。起動時は空、最後の 1 枚も閉じられるようにした"
    },
    {
      "symptom": "ロールが 1 面でしか開けないように見える",
      "cause": "2 面の仕組みは入っていたが、Win A / Win B の「左右に並べる」に当たるボタンがロール側に無く、ツリーの選択先も見えなかった。ヘッダーに 2 画面ボタンと「選択先」表示を追加"
    }
  ],
  "openIssues": [
    {
      "id": "unverified-on-real-app",
      "severity": "high",
      "title": "2026-08-25 の変更は実機で一度も確認していない",
      "detail": "ブラウザで動くところまでは 2026-08-26 にヘッドレス Chrome で確認済み。残るのは File System Access API のネイティブダイアログを伴う操作 (実フォルダ選択・保存・リネーム・削除) のみで、これは人手でしか確認できない。",
      "verifiedOk": [
        "npx tsc --noEmit / npm test (155 件) / npm run build",
        "本番バンドルのパスワードハッシュ照合 (デプロイの都度)",
        "npx tsc --noEmit / npm test (181 件) / npm run build",
        "本番ビルドをヘッドレス Chrome で読み込み、メイン UI が描画されコンソールのエラー・警告がゼロであること (2026-08-26)",
        "本番ビルドで Ctrl+Alt+<文字> が右パネルを開閉しないこと / Ctrl+Alt の成立でトグルすること (2026-08-26)",
        "フォルダ未オープンで再生を押しても UI が壊れず NaN が出ないこと (2026-08-26)",
        "消しゴム・ゴミ取り・バケツ塗りが encodeTGA / decodeTGA の往復を越えて保持されること (saveRoundTrip.test.ts)",
        "表示倍率のログと自動フィットを実ブラウザで確認 (2026-08-31): 読み込み時の自動フィット (100%→300%)、ホイール (300%→330%)、サイズの違うコマへ送ったときに「330% のまま保つ」が記録される。旧実装では同じ操作で 330%→300% に戻されており、報告の「移動時に倍率が変わる」を再現・修正した",
      "報告いただいた実データの形 (ATO_OP_029_trace / ATO_OP_029_paint、_sheet に連番でないファイル + a に連番) を実ブラウザで確認 (2026-08-31): 9 枚 + 9 枚が 9 コマに揃って対になり、コマ送りで常に同じセルが並ぶ。修正前は統合が 10 コマになり a0001 だけ相手が居なかった",
      "2 画面連動の追跡ログを実ブラウザで確認 (2026-08-31): Win B を 2 枚目にして連動 → ↓↓↑↑↑ の操作が、「対にした 2 枚」「キー ↓ / ↑」「主導した面」「A と B の移動前後 (位置とファイル名)」まで 1 行ずつ残り、端で切り詰められた並びを誤って警告しないことを確認",
      "DEBUG ログのウィンドウを実ブラウザで確認 (2026-08-31): ドロップ / 2 画面 / 連動 / コマ送り / ロールの選択連動が時刻付きで残り、全文コピーでヘッダー (日時・件数・URL・UA) 付きのテキストが得られる。クリップボードが使えない環境ではボタンが赤くなりコンソールへ本文を出す (ページは止まらない)",
      "カットを丸ごと Win A へ・その中の _go を Win B へ開いた形を実ブラウザで確認 (2026-08-31): 連動して ↓ を押すと Win A と Win B に必ず同じセル (y0001→y0002→y0003) が並ぶ。修正前は B の y0001 が A の _bg/x0001 と対になっていた",
      "サブフォルダをまたぐコマ送りの順番を実ブラウザで確認 (2026-08-31): _bg 3 枚 + _go 5 枚を開いて ↓ を 8 回押すと、_bg/x0001→x0002→x0003→_go/y0001→…→y0005 とツリーの順どおりに進み、途中で別フォルダへ飛ばない",
      "連動中に Win B のフォルダを開き直しても記録どおりに並ぶことを実ブラウザで確認 (2026-08-29): 先頭コマを持たない retake フォルダを開いても Win B は Win A と同じ位置 (実体が無ければ NO DATA) になり、↑ ↓ でずれない。連動していないときに開いた Win B は Win A と同じコマから始まる",
      "TGA 2 画面でコマ差 -1 のまま先頭をまたぐ操作を実ブラウザで確認 (2026-08-29): 端で止まったあと戻っても Win B が 0001 を飛ばさず、コマ差 -1 が保たれる",
      "TGA 2 画面の連携を実ブラウザで確認 (2026-08-29): 異名連番・同名連番・片側だけリテイク (枚数違い) の 3 通りで、ツリーの選択・連動のコマ差・↑ ↓ の追従・両ウィンドウの表示 (canvas の画素) が一致。ロールを開いたまま TGA フォルダを開いても ↑ ↓ がセルへ効き、ツリーの ← → でフォルダを開閉できることも確認",
      "ロールの ← → コマ送りが 2 面いっしょに動くことを実ブラウザで確認 (2026-08-29): → 3 回で 2 本とも同じ時刻へ進み、← で 2 本とも 1 コマ戻る。面の ▶ ボタンはその面だけが動く",
      "ロールのキー操作を実ブラウザで確認 (2026-08-29): ↑ ↓ で前後のロールへ移り、選択連動中は 2 面いっしょに動く。Space で 2 面が同時に再生され、もう一度で両方止まる。セルのツリーでコマを選ぶと ↑ ↓ はセルへ戻り、ロールは動かない",
      "ロール 2 面のツリーの選択連動を実ブラウザで確認 (2026-08-29): 本番ビルドに 3 本の映像をまとめて落とし、2 画面ボタンでロール B を開き、『選択連動』を入れてロール A のツリーで 3 本目を選ぶとロール B も 3 本目へ移動。連動を切ると片方だけ動く。コンソールのエラー・警告はゼロ",
      "撮影ロールの再生を実映像で確認 (2026-08-27): .mp4 と .mov の双方で readyState=4 / 尺 1.966s / 320px / デコードエラー無し、本物のクリックで再生・一時停止・タイムコード更新、コマ送りも前後とも動作",
        "ロールの D&D を実映像で確認 (2026-08-27): ロールウィンドウへの直接ドロップと、ロールウィンドウを閉じた状態で Win A へ落として自動で開く経路の両方が動作"
      ],
      "nextStep": "コピーしたフォルダで (1) 階層のあるカットの保存 (2) D&D したフォルダへの保存 (3) 連番リネーム (4) サブフォルダをまたぐ複数選択のリネーム (5) 削除・複製 (6) ツリーの選択が飛ばないこと (7) 消しゴム→保存→開き直しで消えたままか を順に確認する。"
    },
    {
      "id": "winb-tools",
      "severity": "medium",
      "title": "Win B でツールが使えない (実体は複数、順に対処済み・最終確認待ち)",
      "detail": "『WinB に対してツールを使用しようとすると WinA に判定が吸われる』の報告。原因は 1 つではなく、表示上の誤認 2 件 + ファイル解決の不具合 2 件 + 選択インデックスのズレ 1 件 + 投げ縄プレビューの左固定 1 件だった。いずれも修正・デプロイ済み (最新は 2026-08-26)。",
      "verifiedOk": [
        "canvas の ref と handleMouseDown(e, false) の配線",
        "targetImg = isLeftView ? currentImage : splitImage の分岐",
        "Win A は Win B の DOM 上・React ツリー上の親ではない (兄弟)。CellWindow はポータルを使っていない",
        "currentImage と splitImage は別オブジェクト (cloneTGAImage を通す)",
        "saveUndoState / jumpToHistory / commitLiveState はすべて activeViewIndex で分岐している",
        "useFloatingWindow の windowStyle はドッキング中 undefined。useFastDraggable は enabled:false で transform を消す",
        "CellWindow のオーバーレイは全て pointer-events-none",
        "handleMouseMove / handleMouseUp / handleWheel はすべて isLeftView で分岐している",
        "setActiveViewIndex は handleMouseDown の先頭で呼ばれ、saveUndoState は get() で新しい値を読む"
      ],
      "nextStep": "実機で再現するか確認する。特に投げ縄 (closedFill / lasso) を Win B で引いて、輪郭線が Win B に出ることを見る。まだ他のツールで起きるなら (1) Win B のタイトルバーに * が付くか (2) 保存ボタンが「未保存 B」になるか を見て切り分ける。"
    },
    {
      "id": "colorchart-lag",
      "severity": "medium",
      "title": "ColorChart の独立ウィンドウが重い (対策済み・効果未確認)",
      "lastAttempt": "document.body へポータル描画 (FloatingPortal.tsx)。右サイドパネルの overflow-y-auto 内にあると position:fixed でも再描画が起きるため。",
      "nextStep": "まだ重い場合は推測で手を打たず、DevTools の Performance で 1 フレームの内訳を実測してから判断する。",
      "investigated": "ColorChart.tsx:68,137 に usePaintStore.setState((state) => ({ ...state })) があり、ストア全体を購読する全パネルを強制再描画する。ただし発火は色のドロップ登録と右クリック登録のみで、ドラッグ中に毎フレーム走る経路ではない。継続的な重さの説明にはならない。"
    }
  ]
}
